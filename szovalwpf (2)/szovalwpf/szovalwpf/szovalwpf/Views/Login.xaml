<Window x:Class="CipoBoltAdmin.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Admin belépés"
        Height="500"
        Width="360"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F3F4F6">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Padding="16" Background="White" CornerRadius="8" BorderBrush="#E5E7EB" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    
                </Grid.RowDefinitions>

                <TextBlock Text="Admin bejelentkezés"
                           FontSize="18"
                           FontWeight="Bold"
                           Margin="0,0,0,14"/>

                <StackPanel Grid.Row="1">
                    <TextBlock Text="Email" Margin="0,0,0,4"/>
                    <TextBox x:Name="TxtEmail" Height="30"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Margin="0,10,0,0">
                    <TextBlock Text="Jelszó" Margin="0,0,0,4"/>
                    <PasswordBox x:Name="TxtPassword" Height="30"/>
                </StackPanel>

                <Button Grid.Row="3"
                        Content="Belépés"
                        Height="34"
                        Margin="0,14,0,0"
                        Click="BtnLogin_Click"/>
                <TextBlock Grid.Row="4" HorizontalAlignment="Center" Margin="0,10,0,0">
                    <Hyperlink Click="LnkRegister_Click">Még nincs fiókom</Hyperlink>
                </TextBlock>

            </Grid>
        </Border>
    </Grid>
</Window>
