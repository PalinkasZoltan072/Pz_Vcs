<UserControl x:Class="CipoBoltAdmin.Views.FelhasznalokView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Felhasználók listája"
                       FontSize="16"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"/>

            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">

                <Button Content="Törlés" MinWidth="100" Height="34" Margin="0,0,10,0" Click="BtnDeleteUser_Click"/>
                <Button Content="Módosítás" MinWidth="120" Height="34" Click="BtnModifyUser_Click"/>
            </StackPanel>
        </Grid>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Felhasznalok}"
                  SelectedItem="{Binding SelectedFelhasznalo}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto">

            <DataGrid.Resources>
                <Style x:Key="TrimmedText" TargetType="TextBlock">
                    <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                    <Setter Property="ToolTip" Value="{Binding Text, RelativeSource={RelativeSource Self}}"/>
                    <Setter Property="Margin" Value="4,0"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>

                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="2.5*" ElementStyle="{StaticResource TrimmedText}"/>
                <DataGridTextColumn Header="Felhasználónév" Binding="{Binding Felhasznalonev}" Width="1.5*" ElementStyle="{StaticResource TrimmedText}"/>

                <DataGridTemplateColumn Header="Jelszó" Width="Auto" MinWidth="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="******" VerticalAlignment="Center" Margin="5,0"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Település" Binding="{Binding Telepules}" Width="*" ElementStyle="{StaticResource TrimmedText}"/>

                <DataGridTextColumn Header="Szállítási cím (Település, Utca, Házszám)" Binding="{Binding Telepules}" Width="*" ElementStyle="{StaticResource TrimmedText}"/>
                <DataGridTextColumn Header="Irányítószám" Binding="{Binding Iranyitoszam}" Width="Auto" MinWidth="90"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>